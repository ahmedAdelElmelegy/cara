import 'package:flutter/material.dart';

class BlogMobileScreen extends StatelessWidget {
  const BlogMobileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Blog Mobile");
  }
}
